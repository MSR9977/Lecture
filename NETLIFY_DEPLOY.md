# 🚀 دليل رفع المشروع على Netlify

## الطريقة 1: رفع يدوي (Drag & Drop) - الأسهل ⭐

### الخطوات:
1. افتح [netlify.com](https://www.netlify.com)
2. اضغط **"Add new site"** → **"Deploy manually"**
3. اسحب مجلد `Lecture` كاملاً إلى الصفحة
4. انتظر حتى ينتهي الرفع
5. احصل على رابط الموقع (مثل: `https://your-site-name.netlify.app`)

✅ **مزايا:**
- سريع وسهل
- لا يحتاج Git
- مباشر

❌ **عيوب:**
- يدوي (كل تحديث يحتاج رفع يدوي)

---

## الطريقة 2: رفع عبر Git (موصى به للمشاريع الكبيرة)

### الخطوات:

#### 1. إنشاء Git Repository
```bash
cd /home/msr/Desktop/JavaScript/Lecture
git init
git add .
git commit -m "Initial commit: Programming Academy Website"
```

#### 2. رفع على GitHub
```bash
# أنشئ repository جديد على GitHub
# ثم نفذ:
git remote add origin https://github.com/YOUR_USERNAME/YOUR_REPO_NAME.git
git branch -M main
git push -u origin main
```

#### 3. ربط مع Netlify
1. افتح [netlify.com](https://www.netlify.com)
2. اضغط **"Add new site"** → **"Import an existing project"**
3. اختر **GitHub**
4. اختر الـ repository الخاص بك
5. إعدادات البناء:
   - **Build command**: اتركه فارغ
   - **Publish directory**: `.` (نقطة فقط)
6. اضغط **"Deploy site"**

✅ **مزايا:**
- تحديث تلقائي عند كل push
- نسخ احتياطية
- إدارة احترافية

---

## الطريقة 3: استخدام Netlify CLI

### التثبيت:
```bash
npm install -g netlify-cli
# أو
pnpm install -g netlify-cli
```

### تسجيل الدخول:
```bash
netlify login
```

### رفع الموقع:
```bash
cd /home/msr/Desktop/JavaScript/Lecture
netlify deploy
```

سيُطلب منك:
1. **Create & configure a new site**: نعم
2. **Team**: اختر فريقك
3. **Site name**: اختر اسماً فريداً
4. **Publish directory**: اكتب `.` (نقطة)

### للرفع النهائي (Production):
```bash
netlify deploy --prod
```

✅ **مزايا:**
- سريع ومباشر
- تحكم كامل
- معاينة قبل النشر

---

## 📁 هيكل المشروع (جاهز للرفع)

```
Lecture/
├── INDEX.html              # الصفحة الرئيسية
├── about.html              # صفحة إعادة توجيه
├── about-lanyard/          # تطبيق Lanyard المبني
│   ├── about.html          # التطبيق التفاعلي
│   ├── assets/             # الصور والملفات
│   └── ...
├── css/                    # ملفات CSS
├── js/                     # ملفات JavaScript
├── assets/                 # الصور
├── netlify.toml            # إعدادات Netlify ✅
└── ...
```

---

## ⚙️ ملف netlify.toml (تم إنشاؤه تلقائياً)

```toml
[build]
  publish = "."

[[redirects]]
  from = "/about"
  to = "/about.html"
  status = 200
```

هذا الملف يضمن عمل جميع الروابط بشكل صحيح.

---

## 🎯 الرابط النهائي

بعد الرفع، ستحصل على رابط مثل:
```
https://your-site-name.netlify.app
```

يمكنك الوصول للبطاقة التفاعلية عبر:
```
https://your-site-name.netlify.app/about.html
```

---

## 🔧 نصائح مهمة:

### 1. تغيير اسم الموقع:
- في لوحة تحكم Netlify
- **Site settings** → **Change site name**

### 2. ربط دومين خاص:
- **Domain settings** → **Add custom domain**

### 3. تفعيل HTTPS:
- تلقائي مع Netlify
- مجاني 100%

---

## 🐛 حل المشاكل الشائعة:

### المشكلة: الصفحة لا تفتح
**الحل:**
- تأكد أن `publish directory` هو `.`
- تأكد أن الملفات في المجلد الصحيح

### المشكلة: البطاقة التفاعلية لا تعمل
**الحل:**
- تأكد أن مجلد `about-lanyard` موجود
- تحقق من مسارات الملفات

### المشكلة: الصور لا تظهر
**الحل:**
- تأكد من رفع مجلد `assets` كاملاً
- تحقق من المسارات النسبية

---

## 📞 المساعدة

إذا واجهت أي مشكلة:
1. تحقق من لوحة **Deploy log** في Netlify
2. تأكد من رفع جميع الملفات
3. راجع هذا الدليل

---

**✨ مبروك! موقعك جاهز للنشر على الإنترنت! ✨**
